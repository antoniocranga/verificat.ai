# API URL — base URL of the verificat.xyz API (set at build time)
API_URL=http://localhost:3000

# Web host — base URL of the verificat.xyz web app (used for privacy policy link, etc.)
VITE_WEB_HOST=https://verificat.xyz
