# API URL — base URL of the Verificat API (set at build time)
API_URL=http://localhost:3000

# Web host — base URL of the Verificat web app (used for privacy policy link, etc.)
VITE_WEB_HOST=https://verificat.xyz
