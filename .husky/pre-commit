pnpm exec lint-staged

# Check for staged secrets locally using Gitleaks if installed
if command -v gitleaks &> /dev/null; then
  gitleaks protect --staged --verbose
else
  echo "Warning: gitleaks CLI is not installed locally. Skipping pre-commit secret detection scan."
fi
